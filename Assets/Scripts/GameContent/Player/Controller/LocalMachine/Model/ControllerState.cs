namespace GameContent.Player.Controller.LocalMachine.Model
{
    public enum ControllerState : byte
    {
        Idle,
        Move,
        Interact,
        Possess,
        Menu,
        Locked
    }
}